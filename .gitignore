# Dependencies
node_modules/

# Build output & tooling caches
dist/
build-helpers/

# Bundled uBlock Origin (GPL) — fetched on demand, not vendored. See scripts/fetch-ublock.js
extensions/

# Logs & OS cruft
*.log
.DS_Store
Thumbs.db

# Local runtime data (never in the project tree, but just in case)
cream-settings.json
cream-vault.dat
